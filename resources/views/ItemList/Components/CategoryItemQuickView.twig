<script type="x/template" id="vue-add-category-item-quickview">
    <div>
        <div id="quickView_TopLeft"></div>
        <div id="quickView_TopRight"></div>
        <div id="quickView_Bottom"></div>
    </div>
</script>

<script>

  function _typeof(obj) { if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

  Vue.component("category-item-quickview", {
    template: "#vue-category-item-quickview",
    delimiters: ["${", "}"],
    props: {

    },
    computed: {

    },
    data: function data() {

    },
    methods: {

    },
    watch: {

    }
  });
</script>
