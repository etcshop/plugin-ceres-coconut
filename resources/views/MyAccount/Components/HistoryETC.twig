{{ component( "Ceres::MyAccount.Components.OrderHistory" ) }}
{{ component( "Ceres::MyAccount.Components.OrderReturnHistory" ) }}

<script type="x/template" id="vue-historyETC">
	<div class="page-content basket history container-max m-b-3 col-lg-12 col-md-12 col-sm-12">
		<ul class="nav nav-tabs" role="tablist">
			<li class="nav-item">
				<div class="ContentCaption">{{ trans("Ceres::Template.orderHistory") }}</div>
				<div class="ContentText">{{ trans("CeresCoconut::Template.ETC_Autraege_Text") }}</div>
			</li>
			<li class="nav-item" v-if="isReturnActive && contactHasReturns">
				<a @click="returnsTabsOpened()" class="nav-link" data-toggle="tab" href="#order-return-tab" role="tab" aria-expanded="false">{{ trans("Ceres::Template.returnHistory") }}</a>
			</li>
		</ul>

		<div class="tab-content">
            <div class="tab-pane active test1553" id="order-history-tab" role="tabpanel" aria-expanded="true">
				<order-history template="#vue-order-history">
				</order-history>
			</div>
			 <div class="tab-pane" id="order-return-tab" role="tabpanel" aria-expanded="false" v-if="isReturnActive && contactHasReturns">
                <order-return-history template="#vue-order-return-history"
					:items-per-page="ordersPerPage"
					:show-first-page="{{ config('Ceres.pagination.showFirstPage') }}"
					:show-last-page="{{ config('Ceres.pagination.showLastPage') }}">
				</order-return-history>
            </div>
		</div>
	</div>
</script>
