{% extends getPartial('page-design') %}

{% import "Ceres::PageDesign.Macros.LayoutContainer" as LayoutContainer %}

{% block PartialHead %}
    <meta name="description" content="{{ trans('Ceres::Template.privacyPolicyMetaDescription') }}">
    <meta name="robots" content="{{ config("Ceres.meta.robots_privacy_policy") }}">
    <title>{{ trans("Ceres::Template.privacyPolicy", {"hyphen": ""}) }} | {{ config("Ceres.header.company_name") }}</title>
{% endblock %}

{% block PageBody %}

    {% set privacyPolicy = services.legalInformation.getPrivacyPolicy() %}

    <div class="container-max page-content">
        <div class="row">
            <div>
                {% autoescape false %}
                    <h1 class="StaticPages_Headline">{{ trans("Ceres::Template.privacyPolicy", {"hyphen": "&shy;"}) }}</h1>
                {% endautoescape %}

                {% if privacyPolicy.htmlText %}
                    {{ privacyPolicy.htmlText | raw }}
                {% else %}
                    <p class="static-page-plain-text">{{ privacyPolicy.plainText }}</p>
                {% endif %}

                {{ LayoutContainer.show("Ceres::StaticPages.PrivacyPolicy") }}
            </div>
        </div>
    </div>

{% endblock %}
