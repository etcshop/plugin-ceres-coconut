<script type="x/template" id="vue-salutation-select">+
    <div style="display: none">Test1113</div>
    <select :value="addressData.addressSalutation" class="custom-select" @change="emitInputEvent(parseInt($event.target.value))">
        <option :value="salutation.id" :selected="addressData.gender === mapSalutationIdToGender(salutation.id) && checkGenderCompany(salutation.id)" v-for="(salutation, index) in currentSalutation">
            ${ salutation.value }
        </option>
    </select>
</script>
