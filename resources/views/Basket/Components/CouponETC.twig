<script type="x/template" id="vue-couponETC">
    <div class="cmp cmp-coupon" id="chk_GutscheinCode">

        <div class="input-group form-group col-lg-12" id="chk_CouponInactive">
            <label class="hidden-xs" for="checkoutCoupon">{{ trans("CeresCoconut::Template.ETC_GutscheinCode") }}</label>
            <input type="text" class="form-control" id="checkoutCoupon" v-model="couponCode" placeholder="{{ trans("Ceres::Template.couponEnterCoupon") }}" @keyup.enter="redeemCode()" :disabled="disabled">
                <button class="btn btn-primary btn-medium btn-block" type="button" @click="redeemCode()" :disabled="waiting" v-if="!disabled">
                    {{ trans("Ceres::Template.couponRedeem") }}
                </button>
                <button class="btn btn-danger btn-medium btn-block" type="button" @click="removeCode()" :disabled="waiting" v-else>
                    {{ trans("Ceres::Template.couponRemove") }}
                </button>
        </div>
    </div>
</script>
